

calculateSH <- function(clustervector, data){
  
  


for i in data$clusters

# calculate a(o) average distance between object o and the objects in its cluster A
  a = 1/ data_cluster1 sum(dist())
# calculate b(o) average dostance between object o and objects in its second closest cluster B

  b = min(1/)
  

# calcualte silhouette of o
s = 0
if(a != 0) {
  s = b -a / (max{a,b})
}

result = 1/size of cluster sum(1/specic cluster )* s(o)
  
}



